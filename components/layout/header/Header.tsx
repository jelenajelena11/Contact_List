import React from "react";
import { HeaderTitle, HeaderWrapper } from "./Header.styled";

export default function Header() {
  return (
    <HeaderWrapper>
      <HeaderTitle>Typeqast</HeaderTitle>
    </HeaderWrapper>
  );
}
